import { } from 'react';
import styles from './MainBoard.module.css';

const MainBoard = () => {
  return (
    <main className={styles.mainboard}>
      MainBoard
    </main>
  );
};

export default MainBoard;